name: Create containers
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: read

jobs:
  push_to_registry:
    permissions:
      packages: write # for docker/build-push-action
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        distro: [arch, centos, debian, fedora, ubuntu]
        runner: [ubuntu-latest]
        cross: [null]
        include:
          - distro: debian
            runner: ubuntu-latest
            cross: i386
          - distro: debian
            runner: ubuntu-24.04-arm
            cross: null
          - distro: debian
            runner: ubuntu-24.04-arm
            cross: s390x
          - distro: precommit
            runner: ubuntu-24.04-arm
            cross: null
    steps:
      - name: Check out the repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: "Generate Dockerfile"
        env:
          MATRIX_CROSS: ${{ matrix.cross || '' }}
          TARGET_DISTRO: ${{ matrix.distro }}
          RUNNER_ARCH: ${{ runner.arch }}
        run: ./contrib/ci/generate_docker.py
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0
      - name: Login to GitHub Container Registry
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Push to GitHub Packages
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        with:
          context: .
          push: true
          tags: "ghcr.io/fwupd/fwupd/fwupd-${{ matrix.distro }}-${{ case(runner.arch == 'ARM64', 'arm64', 'amd64') }}${{ case(matrix.cross != null, format('-cross-{0}', matrix.cross), '') }}:latest"
