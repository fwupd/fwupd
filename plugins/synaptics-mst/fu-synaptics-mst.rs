#[derive(ToString)]
enum SynapticsMstMode {
    Unknown,
    Direct,
    Remote,
}
#[derive(ToString)]
enum SynapticsMstFamily {
    Unknown,
    Tesla,
    Leaf,
    Panamera,
    Cayenne,
    Spyder,
}
